package io.atworks.specscan.analysis.domain.recipe;

public enum BindingType {
    NODE, NODE_SET, PREDICATE, FAILURE, COMPARISON, TARGET, TARGET_PATH, LITERAL, OPERATOR, EVIDENCE, CONSTRAINT, STATUS, UNKNOWN
}
